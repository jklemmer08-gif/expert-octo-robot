# Core ML (pre-installed in base image)
# torch==2.8.0 (from runpod/pytorch base image, CUDA 12.8.1)
# torchvision (matching version from base image)

# Upscaling
realesrgan
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8

# Web UI
flask>=3.0

# Utilities
numpy<2
Pillow
opencv-python-headless

# Optional: TensorRT acceleration (auto-installed on RunPod GPU pods)
# tensorrt>=10.0
# onnx>=1.14

# Testing
pytest
pytest-mock
